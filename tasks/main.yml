---
- name: Enable the EPEL Repo for PIP Install
  yum:
    name: epel-release
    state: latest

- name: "Installing Basic Tools"
  package: pkg={{ item }} state=installed update_cache=yes
  with_items:
  - git
  - vim
  - libaio
  - numactl
  - sysfsutils
  - unzip
  - wget
  - net-tools
  - python3
  - python-pip

- name: Upgrade OS
  yum:
    name: '*'
    state: latest

- name: "Install Python-pexpect"
  pip:
    name: pexpect

- name: set timezone to UTC
  timezone:
    name: UTC
    hwclock: UTC

- name: "Copy RHEL 7 SIO Files"
  copy:
    src: ./files/EMC-ScaleIO-lia-{{ vxflex_version }}.el7.x86_64.rpm
    dest: /tmp/
    owner: root
    group: root
    mode: 0644

- name: "Install SIO LIA"
  yum:
    name: '/tmp/EMC-ScaleIO-lia-{{ vxflex_version }}.el7.x86_64.rpm'
    state: present

- name: "Copy RHEL 7 SIO Files"
  copy:
    src: ./files/EMC-ScaleIO-mdm-{{ vxflex_version }}.el7.x86_64.rpm
    dest: /tmp/
    owner: root
    group: root
    mode: 0644
  tags:
    - install_mdm_manager

- name: "Install MDM  on {{ hostvars[groups['elasticsearch'][0]] }} and set the host to Master"
  yum:
    name: '/tmp/EMC-ScaleIO-mdm-{{ vxflex_version }}.el7.x86_64.rpm'
    state: present
  environment:
    MDM_ROLE_IS_MANAGER: 1
  when:
    - "'mdm' in group_names"
    - hostvars[groups['mdm'][0]]
  tags:
    - install_mdm_manager
`
